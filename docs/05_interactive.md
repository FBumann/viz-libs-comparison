# 05 — Interactive

The chapter the whole project hinges on: what interactivity survives static
HTML export, and at what API cost.

!!! note "Not yet written"
    Planned coverage:

    - **Defaults** — what works out of the box (see smoke test for a preview)
    - Tooltips with custom content
    - Zoom / pan / reset
    - Selection (click + drag-brush)
    - Linked brushing across charts
    - Legend interactions (toggle visibility)
    - Hover highlighting
    - Evaluation table + verdict

# 04 — Multi-layer

Overlaying multiple marks on one axis. Annotations, dual axes, reference lines.
The chapter where "trace per layer" (Plotly) vs "layer operator" (Altair) vs
"add_glyph" (Bokeh) models diverge most.

!!! note "Not yet written"
    Planned coverage:

    - Scatter + line overlay
    - Dual y-axis
    - Text annotations and arrow callouts
    - Reference lines (mean, threshold)
    - Evaluation table + verdict
